import { defineConfig } from '@adonisjs/vite'

/**
 * Server-side Vite settings. The defaults (buildDirectory 'public/assets' and
 * manifestFile 'public/assets/.vite/manifest.json') already match what
 * server/.gitignore expects, so only overrides would go here.
 */
const viteBackendConfig = defineConfig({})

export default viteBackendConfig
